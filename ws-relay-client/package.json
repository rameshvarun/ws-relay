{
  "name": "ws-relay-client",
  "license": "ISC",
  "version": "0.0.1",
  "description": "A WebSocket relay client.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "dependencies": {
    "loglevel": "^1.7.1",
    "@vramesh/ws-relay-common": "^0.0.1"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "format": "prettier --write src/*.ts",
    "typecheck": "tsc --noEmit",
    "build": "rm -rf lib/ && tsc"
  }
}
